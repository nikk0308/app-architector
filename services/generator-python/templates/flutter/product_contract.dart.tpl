class ${boundary_name} {
  const ${boundary_name}({
    this.kind = '${boundary_kind}',
    this.configPath = '${boundary_config_path}',
    this.owner = '${boundary_owner}',
  });

  final String kind;
  final String configPath;
  final String owner;
}
