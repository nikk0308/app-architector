/// ${boundary_description}
class ${boundary_pascal} {
  static const role = '${boundary_role}';
  static const owner = '${boundary_owner}';
  static const dependencies = '${boundary_dependencies}';
}

