export type ${boundary_pascal} = {
  kind: "${boundary_kind}";
  configPath: string;
  owner: string;
};

export const ${boundary_name}: ${boundary_pascal} = {
  kind: "${boundary_kind}",
  configPath: "${boundary_config_path}",
  owner: "${boundary_owner}",
};
