namespace ${project_pascal}.Config
{
    public static class AppConfig
    {
        public const string BundleId = "${package_id}";
        public const string EnvironmentMode = "${environment_mode}";
    }
}
