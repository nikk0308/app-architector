namespace ${project_pascal}.Architecture
{
    /// <summary>
    /// ${boundary_description}
    /// </summary>
    public static class ${boundary_pascal}
    {
        public const string Role = "${boundary_role}";
        public const string Owner = "${boundary_owner}";
        public const string Dependencies = "${boundary_dependencies}";
    }
}

