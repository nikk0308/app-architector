namespace ${project_pascal}.Product
{
    public sealed class ${boundary_name}
    {
        public string Kind => "${boundary_kind}";
        public string Role => "${boundary_role}";
        public string ConfigPath => "${boundary_config_path}";
        public string Dependencies => "${boundary_dependencies}";
    }
}
