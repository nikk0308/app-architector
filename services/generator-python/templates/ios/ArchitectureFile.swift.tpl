import Foundation

/// ${boundary_description}
struct ${boundary_pascal} {
    static let role = "${boundary_role}"
    static let owner = "${boundary_owner}"
    static let dependencies = "${boundary_dependencies}"
}

