{
  "kind": "delivery-boundary",
  "project": "${project_name}",
  "profile": "${profile}",
  "options": "${delivery_options}",
  "checkpoints": [
    "ci build",
    "test plan",
    "release checklist",
    "environment secrets",
    "design system baseline"
  ]
}
