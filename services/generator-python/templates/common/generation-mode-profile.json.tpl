{
  "schemaVersion": "1.0",
  "generationMode": "${generation_mode}",
  "modeDisplayName": "${mode_display_name}",
  "strategy": "${mode_strategy_summary}",
  "relationship": "${mode_relationship_summary}",
  "materialization": "AI may synthesize or refine structured architecture data, but the ZIP file tree is materialized by the deterministic generator."
}
