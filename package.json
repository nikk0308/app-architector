{
  "name": "mobile-architecture-generator",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @mag/api",
    "dev:api": "npm run dev -w @mag/api",
    "dev:web": "npm run dev -w @mag/web",
    "build": "npm run build -w @mag/shared && npm run build -w @mag/api && npm run build -w @mag/web",
    "test": "npm run test -w @mag/shared && npm run test -w @mag/api",
    "lint": "npm run lint -w @mag/api && npm run lint -w @mag/web",
    "start": "npm run start -w @mag/api"
  }
}
