{
  "name": "@mag/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -w @mag/shared && vite",
    "build": "npm run build -w @mag/shared && tsc --noEmit && vite build",
    "lint": "npm run build -w @mag/shared && tsc --noEmit"
  },
  "dependencies": {
    "@mag/shared": "0.1.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.0.4",
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}