using UnityEngine;

namespace ${project_pascal}.Core
{
    public class AppManager : MonoBehaviour
    {
        private void Awake()
        {
            Debug.Log("Bootstrapping ${display_name} with ${architecture_style} architecture.");
        }
    }
}
